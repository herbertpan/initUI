<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="#FFFFFF" />
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:topLeftRadius="@dimen/othersinfo_avatar_radius"
             android:topRightRadius="@dimen/othersinfo_avatar_radius"
             android:bottomLeftRadius="@dimen/othersinfo_avatar_radius"
             android:bottomRightRadius="@dimen/othersinfo_avatar_radius"/>

    <!--gradient
        android:angle="270"
        android:centerColor="#0000FF"
        android:endColor="#0000FF"
        android:startColor="#0000FF" -->

</shape>